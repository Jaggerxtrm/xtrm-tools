# Project Memory — xtrm-tools
_Updated: 2026-04-02 | Bootstrapped for tracking_

## Architecture & Decisions
- Keep this file committed and updated via `xt memory update`; treat it as injected system context, not runtime cache.

## Non-obvious Gotchas
- Write guidance here as directives (`Do X`, `Never do Y`), not descriptive documentation.

## Process & Workflow Rules
- Before making workflow-heavy changes, refresh this file through `xt memory update` so fresh sessions start with current directives.
