---
session_date: 2026-04-13
branch: main
commits: 3
issues_closed: 3
issues_filed: 5
specialist_dispatches: 8
models_used: [gpt-5.3-codex, qwen3.5-plus]
---

# Session Report — 2026-04-13

## Summary

Fixed two bugs and shipped one feature across xtrm-tools and specialists repos. The session started with investigating YouTube transcript extraction in the last30days skill, which revealed that `youtube_yt.py` hardcodes `--sub-lang en` — breaking transcript fetch for non-English videos. An executor fixed this (xtrm-tools `c3aa73ad`). While testing `sp merge` to publish the fix, the recurring "empty merge delta" bug surfaced again. Root cause investigation found two issues in `specialists/src/cli/merge.ts`: (1) `previewBranchMergeDelta()` runs git commands from `process.cwd()` instead of the main worktree root, causing self-merge when invoked from a worktree; (2) the preview uses `git merge --no-commit` which fails on dirty working trees. Fixed by adding `resolveMainWorktreeRoot()` and replacing the mutating preview with a read-only `git merge-base` + `git diff` comparison (specialists `ae22854d`). Three consecutive reviewer FAILs exposed a P0 bug: the reviewer specialist resolves evidence by crawling `.specialists/jobs/` filesystem artifacts instead of using `sp result`/`sp ps` CLI — those files don't exist in worktrees. Finally, added Mode 3 (media research) to the researcher specialist, enabling YouTube transcript extraction via yt-dlp. Tested on an Italian livestream — qwen extracted 35k subtitle lines and produced a structured content analysis with translated quotes (specialists `336f0c11`).

## Issues Closed (3)

### Bugs Fixed
| ID | Title | Specialist | Resolution |
|----|-------|-----------|------------|
| xtrm-uymp | Fix YouTube transcript fetch for non-English videos | executor (gpt-5.3-codex) | `--sub-lang en` → `--sub-lang all`, language-aware track candidates in direct HTTP path, VTT glob now language-agnostic |
| xtrm-3kpo | Fix sp merge empty delta: previewBranchMergeDelta runs from wrong cwd | executor (gpt-5.3-codex) | Added `resolveMainWorktreeRoot()` via `git worktree list --porcelain`, replaced mutating preview with `git merge-base` + `git diff` |

### Features
| ID | Title | Specialist | Resolution |
|----|-------|-----------|------------|
| xtrm-h746 | Researcher: add last30days transcript pipeline as Mode 3 | — (orchestrator) | Added Mode 3 media research to researcher prompt, bumped LOW→MEDIUM perms, tested on Italian livestream |

### Cleanup
| ID | Title | Resolution |
|----|-------|------------|
| xtrm-ok9g | Fix researcher skill path: last30days uses default/ instead of active/pi/ | Corrected path in config and synced canonical → active |

## Issues Filed

| ID | P | Type | Repo | Title | Why |
|----|---|------|------|-------|-----|
| unitAI-kek2 | P0 | bug | specialists | Reviewer uses file-based lineage instead of sp result/sp ps CLI | 3/3 reviewer runs FAILed reading stale `executor-result.md` from prior tasks instead of the actual diff. Reviewer prompt never mentions `sp result` or `sp ps`. |
| unitAI-i2tv | P1 | feature | specialists | Flatten skills path: merge default/ and active/pi/ into single active/ root | Researcher had wrong skill path (default/ vs active/pi/) — the two-layer structure causes config mismatches and double-bookkeeping |
| xtrm-avqb | P1 | feature | xtrm-tools | Flatten .xtrm/skills/ path: remove default/active/pi layering | Companion to unitAI-i2tv. xtrm-tools owns the directory structure and install/sync logic |
| transcriptoz-b4q | P2 | feature | transcriptoz | Agent pipeline: yt-dlp transcript extraction as lightweight whisperx alternative | Researcher Mode 3 extracts transcripts in seconds vs whisperx's GPU-heavy approach. Build pipeline around this for videos with auto-captions |
| xtrm-h746 | P2 | feature | xtrm-tools | Researcher: add last30days transcript pipeline as Mode 3 | Researcher had last30days skill loaded but prompt never referenced it and LOW perms blocked script execution |

## Specialist Dispatches

Session jobs: **8** (done:8)

### Waves

| Wave | Specialist | Model | Job | Outcome |
|------|-----------|-------|-----|---------|
| 1: YT transcript fix | executor | gpt-5.3-codex | 555e00 | PASS — fixed `--sub-lang en` and VTT discovery |
| 1: YT transcript fix | reviewer | gpt-5.3-codex | 9d82ca | FAIL (false negative — read stale artifact) |
| 2: sp merge fix | executor | gpt-5.3-codex | 7c88d3 | PASS — v1 cwd fix + v2 merge-base preview (2 resumes) |
| 2: sp merge fix | reviewer | gpt-5.3-codex | af1895 | FAIL (same stale artifact bug) |
| 2: sp merge fix | reviewer | gpt-5.3-codex | 90fb3c | FAIL (same stale artifact bug) |
| 3: YT video research | researcher | qwen3.5-plus | af45a7 | PASS — metadata only (pre-Mode 3) |
| 4: Mode 3 test | researcher | qwen3.5-plus | 5aea28 | PASS — 35k subtitle lines extracted, full content analysis |
| — stale (prior session) | reviewer | gpt-5.3-codex | 758c57 | Cancelled — orphaned from prior session |

### Problems

| Problem | Root Cause | Resolution |
|---------|-----------|------------|
| 3/3 reviewer FAILs | Reviewer crawls `.specialists/jobs/` filesystem (doesn't exist in worktrees) instead of using `sp result`/`sp ps` CLI | Filed as unitAI-kek2 P0 |
| Executor edited specialists repo from xtrm-tools worktree | sp merge bead was in xtrm-tools but the target code is in dev/specialists (separate repo) | Committed manually in specialists repo; worktree was orphaned |
| sp merge "empty merge delta" on valid branch | previewBranchMergeDelta uses process.cwd() + mutating git merge preview | Fixed in-session (ae22854d) |

## Code Changes

### xtrm-tools (main) — `c3aa73ad`
- **`.xtrm/skills/default/last30days/scripts/lib/youtube_yt.py`** — Non-English transcript support: `--sub-lang all`, new `_build_caption_track_candidates()` helper for language-prioritized direct HTTP fallback, language-agnostic VTT file discovery

### specialists (master) — `ae22854d`
- **`src/cli/merge.ts`** — Two-part merge fix: added `resolveMainWorktreeRoot()` (parses `git worktree list --porcelain`), replaced mutating `git merge --no-commit` preview with read-only `git merge-base` + `git diff` comparison, threaded `cwd` param through all merge-path functions (`previewBranchMergeDelta`, `mergeBranch`, `runTypecheckGate`, `runRebuild`, `readChangedFilesForLastMerge`, `assertBranchMergeWorthiness`, `getConflictFiles`)

### specialists (master) — `336f0c11`
- **`config/specialists/researcher.specialist.json`** + **`.specialists/default/researcher.specialist.json`** — Added Mode 3 media research (YouTube transcripts via yt-dlp/last30days pipeline), bumped permission LOW→MEDIUM, fixed skill path from `default/last30days` to `active/pi/last30days/SKILL.md`

## Documentation Updates

- bd memory saved: `sp-merge-empty-delta-bug-had-two-root` — documents both root causes and fix approach for the sp merge bug
- Transcript saved: `/home/dawid/dev/transcriptoz/transcripts/ZMawN97EetI.it.srt` — first yt-dlp-extracted Italian transcript

## Open Issues with Context

### Ready for next session

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| unitAI-kek2 | P0 | Reviewer file-based lineage bug | **Critical**: every reviewer dispatch fails. Fix the system prompt in `config/specialists/reviewer.specialist.json` to use `sp result <job-id>` + `sp ps <job-id>` + `git diff` as primary evidence sources instead of crawling `.specialists/jobs/` filesystem. The `--job` flag correctly enters the worktree — the reviewer just doesn't know how to read the executor's output from there. |
| xtrm-avqb | P1 | Flatten skills path (xtrm-tools) | Do this before unitAI-i2tv. Decide: keep `default/` as source + symlink `active/<skill>` → `../default/<skill>`, or make `active/` the single canonical root. Check `cli/lib/sync.js` for install path logic. The `pi/` subdirectory layer under `active/` is the main pain point. |
| unitAI-i2tv | P1 | Flatten skills path (specialists) | Depends on xtrm-avqb landing first. Then update all 14 specialist configs in `config/specialists/*.json` + `.pi/settings.json`. Grep for `active/pi/` to find all references. |

### Backlog

| ID | P | Title | Context / Suggestions |
|----|---|-------|----------------------|
| xtrm-54xq | P1 | Package Pi extensions as npm | Needed for cleaner specialist provisioning |
| xtrm-qeyn | P1 | Block Agent tool when using-specialists active | Prevents orchestrator from self-implementing instead of delegating |
| transcriptoz-b4q | P2 | yt-dlp transcript pipeline in transcriptoz | First transcript saved. Build pipeline: yt-dlp first → whisperx fallback. Researcher Mode 3 is proven. |
| xtrm-n9y4 | P2 | Update using-specialists SKILL.md | Post-executor sequence docs need refresh after review cycle learnings this session |
| xtrm-2yj6 | P2 | Add local model support for specialists | Ollama/LM Studio integration |
| xtrm-a3mn | P2 | Surface GitNexus index as codebase maps | Agent-ready architecture views |
| xtrm-qstt | P3 | Auto-generate PR descriptions from bead + git diff | Quality-of-life for worktree sessions |
| xtrm-p7gv | P3 | Create specialists Q&A knowledge base | Pattern memory across sessions |
| xtrm-16al | P3 | [epic] Project utils system | Typed utility scripts, manifest, process manager |

## Memories Saved

| Key | Content |
|-----|---------|
| sp-merge-empty-delta-bug-had-two-root | sp merge empty-delta had TWO root causes: (1) wrong cwd (process.cwd() vs main worktree), (2) mutating preview fails on dirty working tree. Fix: resolveMainWorktreeRoot() + read-only merge-base diff. Fixed in specialists `ae22854d`. |

## Suggested Next Priority

1. **unitAI-kek2 (P0)**: Fix reviewer specialist prompt — every review dispatch currently fails. This blocks the entire review→fix loop that the specialist workflow depends on. Quick fix: update `prompt.system` in `reviewer.specialist.json`.
2. **xtrm-avqb + unitAI-i2tv (P1)**: Flatten skills paths — do xtrm-tools first (owns dir structure), then specialists (consumes paths). Eliminates the default/active/pi confusion that caused today's researcher path mismatch.
3. **transcriptoz-b4q (P2)**: Build yt-dlp transcript pipeline — researcher Mode 3 is proven, wire it into transcriptoz as whisperx alternative. User indicated this is the next project direction.
