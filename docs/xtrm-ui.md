---
title: XTRM UI Extension
scope: xtrm-ui
category: reference
version: 1.0.0
updated: 2026-03-26
synced_at: eb2a8e3
description: "Pi extension that replaces pi-dex with XTRM-tuned chrome: light theme default, compact tools, header, and footer disabled for custom-footer compatibility"
source_of_truth_for:
  - "config/pi/extensions/xtrm-ui/index.ts"
  - "config/pi/extensions/xtrm-ui/package.json"
  - "config/pi/extensions/xtrm-ui/themes/*.json"
domain: [pi, extensions, ui, themes]
---

<!-- INDEX: auto-generated by validate_metadata.py — do not edit manually -->
| Section | Summary |
|---|---|
| [Overview](#overview) | `xtrm-ui` is a Pi extension that wraps the visual chrome from |
| [Credits](#credits) | Themes and header layout are derived from **pi-dex** by |
| [Why Not pi-dex Directly](#why-not-pi-dex-directly) | pi-dex calls `setFooter()` unconditionally, which races with `custom-footer`'s |
| [Defaults](#defaults) | Preferences are persisted per session via `pi |
| [Commands](#commands) | `xtrm-ui` is a managed extension — it is automatically synced to |
| [Install](#install) | `xtrm-ui` is a managed extension — it is automatically synced to |
<!-- END INDEX -->

<!-- INDEX: auto-generated by validate_metadata.py — do not edit manually -->
| Section | Summary |
|---|---|
| [Overview](#overview) | Replaces pi-dex with XTRM-tuned defaults |
| [Credits](#credits) | Based on pi-dex by @mariozechner |
| [Why Not pi-dex Directly](#why-not-pi-dex-directly) | Footer race condition with custom-footer |
| [Defaults](#defaults) | Light theme, compact, header on, footer off |
| [Commands](#commands) | /xtrm-ui, /xtrm-ui-theme, /xtrm-ui-density, /xtrm-ui-header, /xtrm-ui-reset |
| [Install](#install) | Auto-synced via xtrm install |

---

## Overview

`xtrm-ui` is a Pi extension that wraps the visual chrome from
[pi-dex](https://github.com/mariozechner/lemmy/tree/main/packages/pi-dex) with
XTRM-specific defaults. It provides the pidex header, themes, and tool expansion
settings — but deliberately **omits the footer**, letting `custom-footer` own that
slot exclusively.

## Credits

Themes and header layout are derived from **pi-dex** by
[@mariozechner](https://github.com/mariozechner). The original pi-dex package is
available at `npm:pi-dex` and provides a full-featured UI chrome for the Pi coding
agent. `xtrm-ui` uses the pidex-light and pidex-dark theme JSON files and the
box-drawing header pattern from pi-dex, adapted for XTRM's multi-extension stack.

## Why Not pi-dex Directly

pi-dex calls `setFooter()` unconditionally, which races with `custom-footer`'s
`setFooter()` call — whichever fires last wins, causing the statusline to flicker
or revert to the stock pi-dex footer. `xtrm-ui` sets `showFooter: false` by default
and never calls `setFooter()`, giving `custom-footer` stable ownership of the
statusline.

## Defaults

| Preference | Default | Notes |
|------------|---------|-------|
| `themeName` | `pidex-light` | Switch with `/xtrm-ui-theme dark` |
| `density` | `compact` | Switch with `/xtrm-ui-density comfortable` |
| `showHeader` | `true` | Toggle with `/xtrm-ui-header off` |
| `compactTools` | `true` | Tool blocks collapsed by default |
| `showFooter` | `false` | Intentionally off — custom-footer handles this |

Preferences are persisted per session via `pi.appendEntry` and restored on
`session_start` / `session_switch` / `session_fork`.

## Commands

| Command | Description |
|---------|-------------|
| `/xtrm-ui` | Show current preferences and context usage |
| `/xtrm-ui-theme dark\|light` | Switch theme |
| `/xtrm-ui-density compact\|comfortable` | Switch density |
| `/xtrm-ui-header on\|off` | Toggle header |
| `/xtrm-ui-reset` | Restore all defaults |

## Install

`xtrm-ui` is a managed extension — it is automatically synced to
`~/.pi/agent/extensions/xtrm-ui/` on every `xtrm install`. Pi auto-discovers
extensions in that directory, so no manual `pi install -l` step is required.

pi-dex (`npm:pi-dex`) is **not** installed when using xtrm-ui — it was removed
from `config/pi/install-schema.json` to avoid the footer conflict.
