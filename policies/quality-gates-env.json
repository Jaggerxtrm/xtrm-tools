{
  "id": "quality-gates-env",
  "description": "Verify quality gate tools (tsc, eslint, ruff) are available at session start. Warns agent early if enforcement would be silently degraded.",
  "version": "1",
  "runtime": "claude",
  "order": 41,
  "claude": {
    "hooks": [
      {
        "event": "SessionStart",
        "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/quality-check-env.mjs",
        "timeout": 5000
      }
    ]
  }
}
