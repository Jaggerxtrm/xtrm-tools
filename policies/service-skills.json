{
  "id": "service-skills",
  "description": "Inject service catalog into system prompt at agent start (cataloger.py), and detect skill documentation drift after file edits (drift_detector.py). Runs only when service-registry.json exists in the current project.",
  "version": "1",
  "runtime": "pi",
  "order": 40,
  "pi": {
    "extension": "packages/pi-extensions/extensions/service-skills/index.ts",
    "events": ["before_agent_start", "tool_result"]
  }
}
